import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'beaver-creditreminder',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
